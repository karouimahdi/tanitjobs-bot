// TODO(TKT-007): render the TanitJobs listing page and report HTTP status,
// cf-ray presence, JobPosting JSON-LD presence, observed JSON endpoints, and
// the shape of /job/<id> links. Write raw/probe.html + raw/probe.png.
// See CLAUDE.md — "ALWAYS run before touching selectors. Never guess."

console.log("probe: not implemented yet — see TKT-007.");
