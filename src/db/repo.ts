// TODO(TKT-008): the only module allowed to call getFirestore(). Watermark
// get/set, job create, classification get-or-create, outbox create/update,
// stats increment, profile read, run-telemetry write all live here.

export {};
