// TODO(TKT-029): wire sweep -> dedup -> detail fetch -> prefilter -> classify
// -> score -> outbox -> send into one full cycle, verbosely, then exit.
// This is the entrypoint .github/workflows/collect.yml actually runs.

console.log("once: not implemented yet — see TKT-029.");
